<p class="meta">
    {{ .Date.Format "1/_2, 2006" }} &middot; {{ .File.Dir }}

    {{ if isset .Params "tags" -}}
        {{ $tagsLen := len .Params.tags }}
        {{ if gt $tagsLen 0 -}}
            &middot; Tags:
            {{ range $k, $v := (sort .Params.tags) -}}
            <a href="{{ $.Site.BaseURL }}tags/{{ . | urlize | lower }}">{{ . }}</a>
            {{ if lt $k (sub $tagsLen 1) }}, {{ end }}
            {{- end }}
        {{- end }}
    {{- end }}
</p>
